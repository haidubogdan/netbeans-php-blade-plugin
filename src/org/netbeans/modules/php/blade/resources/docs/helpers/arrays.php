<?php

namespace {
    /**
     * The array_add function adds a given key / value pair to the array if the given key
     * doesn't already exist in the array:
     */
    function array_add(): void {}
    $array = array_add(['name' => 'Desk'], 'price', 100);
    // ['name' => 'Desk', 'price' => 100]
}

